#2、3、7：
因为我想把重点主要放在服务端，所以客户端的界面和操作都设置得很简陋...一开始我在skynet的示例client上做改动，但是发现不能正常获取控制台输入（后来看了下是因为client.socket里用了stdin）。正好我想简单尝试下与服务端的连接，以确保自己的理解没有问题，就用C写了个基本的client socket加上2字节pack。并且由于想让客户端在windows平台运行，又把socket API从Linux换成了Windows。后来基于想与服务端共用部分代码、sproto的使用便捷性等考虑，决定还是重构成C+Lua的版本（原版是Linux，所以就算解决了控制台输入的问题也没法直接用），刚好学习了下Lua和C的交互方式（原来是通过虚拟栈...）。

